quest.test = {
	asserts : function(a, b) {
		if(Array.isArray(a)) {
			for(var i=0, l=a.length; i<l; i++) {
				if(typeof a[i] === 'number') {
					
				}
			}
			return true;
		}
	},
	approx : function(a, b, tol) {

	}
}
